<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="utf-f" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"] - CistacApp</title> @* Fallback title *@

    <link rel="stylesheet" href="~/lib/bootstrap/dist/css/bootstrap.min.css" />
    <link rel="stylesheet" href="~/css/site.css" asp-append-version="true" />
    <link rel="stylesheet" href="~/CistacApp.styles.css" asp-append-version="true" /> @* For component-specific styles *@
    <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@300;400;500;700&display=swap" rel="stylesheet">

</head>

<body>
    <header class="top-navbar">
        <div class="container-fluid d-flex justify-content-between align-items-center">
            @* UPDATED: Logo - Replace placeholder URL with your actual logo image URL or path *@
            <a class="navbar-brand logo" asp-area="" asp-controller="Home" asp-action="Index">
                <img src="https://placehold.co/120x50/007bff/ffffff?text=CistacApp&font=roboto" alt="CistacApp Logo" class="logo-image"> @* Placeholder Logo *@
            </a>
            <nav class="main-menu">
                <ul class="navbar-nav d-flex flex-row">
                    <li class="nav-item"><a class="nav-link" asp-area="" asp-controller="Home" asp-action="Index">HOMEPAGE</a></li>
                    <li class="nav-item"><a class="nav-link" asp-area="" asp-controller="Hotels" asp-action="Index">HOTELS</a></li>
                    <li class="nav-item"><a class="nav-link" asp-area="" asp-controller="Users" asp-action="Index">USERS</a></li>
                </ul>
            </nav>
            @* <div class="user-info">
                @if (User.Identity.IsAuthenticated)
                {
                    
                    <span>Merhaba, @(User.FindFirst("FullName")?.Value ?? User.Identity.Name)!</span>
                    <form class="form-inline" asp-controller="Account" asp-action="Logout" method="post">
                        @Html.AntiForgeryToken()
                        <button type="submit" class="nav-link btn btn-link text-dark user-initials" style="padding: 0; border: none; background: none; text-transform: uppercase; font-weight: 500;">Logout</button>
                    </form>
                }
                else
                {
                    <ul class="navbar-nav d-flex flex-row">
                        <li class="nav-item">
                            
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" asp-controller="Account" asp-action="Login">LOGIN</a>
                        </li>
                    </ul>
                }
            </div> *@
        </div>
    </header>

    <div class="page-title-bar">
        <div class="container-fluid">
            <h2>@(ViewData["PageTitle"] ?? ViewData["Title"])</h2> @* Display specific PageTitle or fallback to general Title *@
        </div>
    </div>

    <div class="main-content container-fluid mt-3 mb-3">
        <main role="main" class="pb-3">
            @RenderBody()
        </main>
    </div>

    @* <footer class="border-top footer text-muted">
        <div class="container">
            &copy; 2025 - CistacApp - <a asp-area="" asp-controller="Home" asp-action="Privacy">Privacy</a>
        </div>
    </footer> *@

    <script src="~/lib/jquery/dist/jquery.min.js"></script>
    <script src="~/lib/bootstrap/dist/js/bootstrap.bundle.min.js"></script>
    <script src="~/js/site.js" asp-append-version="true"></script>
    @await RenderSectionAsync("Scripts", required: false)
</body>
</html>